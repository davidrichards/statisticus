Just a quick note.  Statisticus looks down the file system tree to find any R libraries (*.r).  So, for demonstrating the conventions of Statisticus, it makes sense to put a few demo libs in a subdirectory of the steps, where some classes are defined.